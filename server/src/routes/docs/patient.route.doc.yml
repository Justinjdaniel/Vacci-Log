tags:
  - name: Patient
    description: Everything about your patient
paths:
  /patient:
    get:
      tags:
        - Patient
      summary: Get patient details
      description: Get patient details
      operationId: getPatient
      parameters:
        - name: id
          in: query
          description: The patient unique id
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Patient'
            application/xml:
              schema:
                $ref: '#/components/schemas/Patient'
        '405':
          description: Invalid input
      security:
        - bearerAuth:
            - read
    post:
      tags:
        - Patient
      summary: Add a new patient details to the Blockchain and DB
      description: Add a new patient details to the Blockchain and DB
      operationId: registerPatient
      requestBody:
        description: Data for creating a new patient in the Blockchain and DB
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Patient'
          application/xml:
            schema:
              $ref: '#/components/schemas/Patient'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Patient'
        required: true
      responses:
        '200':
          description: Successful operation
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Patient'
            application/xml:
              schema:
                $ref: '#/components/schemas/Patient'
        '405':
          description: Invalid input
      security:
        - bearerAuth:
            - write
    patch:
      tags:
        - Patient
      summary: Update Patient details by adding vaccine dose details
      description: Adding vaccine dose details
      operationId: addVaccineDose
      requestBody:
        description: Data for updating patient and add vaccine dose details
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/VaccinationDose'
          application/xml:
            schema:
              $ref: '#/components/schemas/VaccinationDose'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/VaccinationDose'
        required: true
      responses:
        '200':
          description: successful operation
          headers:
            X-Rate-Limit:
              description: calls per hour allowed by the user
              schema:
                type: integer
                format: int32
            X-Expires-After:
              description: date in UTC when token expires
              schema:
                type: string
                format: date-time
          content:
            application/xml:
              schema:
                type: string
            application/json:
              schema:
                type: string
        '400':
          description: Invalid username/password supplied
      security:
        - bearerAuth:
            - write
